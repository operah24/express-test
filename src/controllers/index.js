// const {addBook, editBook, deleteBook, getAllBooks, getBookById} = require("./books")
const { addBook, getAllBooks, getBookById, editBook, deleteBook  } = require("./books")

module.exports = {
    // addBook, editBook, deleteBook, getAllBooks, getBookById
    addBook, getAllBooks, getBookById, editBook, deleteBook
}